const AppError = require('../util/AppError');
const db = require('./../model/index');
const catchError = require("./../util/catchError")

const signUp = catchError(req, res, next)=> {
  
}
module.exports = {};
